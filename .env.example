# ===== App Environment =====
# local = แสดง error และ (แนะนำ) สร้าง DB/Table อัตโนมัติ
# production = ปิด debug (แนะนำให้ AUTO_SETUP_DB=0)
APP_ENV=local

# 1 = ให้โค้ดสร้าง Database/Table อัตโนมัติ (ใช้ตอน local)
# 0 = ไม่สร้างอัตโนมัติ (แนะนำตอน production)
AUTO_SETUP_DB=1

# ===== SMTP (Optional) =====
# ถ้าไม่ใส่ ระบบจะ "บันทึกการจองได้" แต่จะไม่ส่งอีเมล
SMTP_USER=yourgmail@gmail.com
SMTP_PASS=your_gmail_app_password

